package cctZoo;

import utils.CSVReader;

public class Main {

	public static void main(String[] args) {
		
	//	CSVReader reader = new CSVReader();
		
	// modelo de como utilizar o CSV reader...
	// o metodo run retorna um ArrayList onde cada elemento é um Array de strings 
	// as quais são os dados de cada linha do arquivo 
		
		// na hora de importar, usar o metodo direto do controller, nao o da facade,
		// pois ja ta vindo em boolean
		
		
//		for (String[] object : reader.run("file.csv")) {
//
//			if (Boolean.parseBoolean(object[0].trim()))
//			System.out.println(object[0] + ", " + object[1]);
			
//		}

	}

}
